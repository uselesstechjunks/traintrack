# Generative Models

## Introduction
What am I trying to achieve?
- To explain what these models can do
- To make people understand the theme of this talk, and which topics come when.

### Slide 1: Motivation
#### Content
- Pictures of images, videos, proteins generated by models (if possible, embed Will Smith Spaghetti video)
- Name a few SOTA models: (image) Midjourney, Stable Diffusion 3 (video) MiniMax H3, Seedance 2.5, Sora,  (protein): AlphaFold 3

#### Notes
- Lately we've seen diffusion models doing wonders in the field of image generation, video generation or even coming up with physical structures in 3D such as novel proteins.
- Many of these models are SOTA in their respective category. Name them from the slide.
- Despite different architectures and modalities, these models share the principles that come from the same mathematical ideas.
- In this talk, we'll focus on understanding those principles and where they come from.

### Slide 2: Roadmap
#### Content
- Scope & overview
- Formulation
- What is the unifying framework?
- What is the flow view?
- How do we generate samples?
- How do we train these models?
- Okay, how do we "actually" train these models?
- How do I prompt?
- Where can I find more?

#### Notes: Contract with the audience
- Scope: Note that some of the generative tasks are being tackled by autoregressive family of models. While they are catching up, the focus of this talk is not on those models.
- The work on diffusion/flow-based models is a huge and rapidly evolving field. What makes it even more complicated is that these class of models have a non-linear history: for example, the mathematical formulation that was used in the earlier set of papers is not what research community follows anymore.
- So, instead trying to attempt a chronological overview, I'm going to take a route that makes more sense pedagogically in the modern view of these models.
- This formulation also makes it easy to discuss other generative modeling approaches (such as WGAN/MMDGAN, which were SOTA for a while) under the same framework.
- After establishing the framework, we will connect it back to the earlier diffusion formulations such as DDPM.
- Note that for a vast part of this talk, we'd assume unconditional generation, i.e., generation without any prompt (which you can think of as having a static prompt).
- Towards the later half, we'd touch upon this, discuss the key idea, and provide pointers to resources for more detailed treatments.

### Slide 3: Formulation
#### Content
- Dataset: $D=(z_i)_{i=1}^N, z_i\in\mathbb{R}^d$.
- Generation: $Z\sim p_{\text{data}}$, sample $Z=z_i$.
- Options
  - 1. Estimate $p_{\text{data}}$ with a parametric model $p_\theta$. Sample from $p_\theta$.
  - 2. Learn a morphism from a known $p_{\text{init}}$ into $p_{\text{data}}$.
       - Sample from $p_{\text{init}}$.
       - Morph the sample.
- The framework that describes the second option: transport problem.

#### Notes
- Regardless of the modality, we think of the data as $d$-dimensional vectors.
- We assume that there's an underlying random variable $Z$ with distribution $p_{\text{data}}$ where these samples are generated from.
- Options: call out the first one. Mention that autoregressive models and normalising flow models fall under this category.
- Second option: read out literally.

### Slide 4: What is the unifying framework?
#### Content: Measuring the Morphing (Minimal OT)
- The Goal: Learn a map $T$ that morphs some distribution $p$ to target $q$.

  $$
  T_\sharp p=q
  $$

- Optimal Transport: For some cost function $c:\mathbb{R}^d\times\mathbb{R}^d\to\mathbb{R}_{\geq 0}$

  $$
  \inf_T\mathbb{E}_{x\sim\mu}[c(x, T(x)]
  $$

- Example:
  - Wasserstein Distance: $c(x, T(x))=(||x-T(x)||_p)^{1/p}$
  - What is the minimum physical effort required to reshape $p$ into $q$.
  - Insert image on the side of stuff moving (source internet)

#### Notes
- TODO

### Slide 5: The Dual Formulation & WGANs
- 

----
## Mathematical foundation
Question: Why we're starting with math? 
- The DDPM/variational view is confusing, and that's not how the SOTA models work -> people understand that diffusion doesn't mean DDPM anymore
- So, we'll start with the mathematical framework that makes it intuitive of what works
- And the framework also allows us to show how the different approaches to generative models connect with one another

Question: What is this framework?
- Monge formulation -> people understand the basic terminology of transport
- Kantorovich relaxation (need to motivate via examples what the coupling refers to) -> people understand what the "joint" achieves
- Wasserstein distance, IPM -> people understand how to measure distance, and how distance itself is formulated using "transport" ideas

Question: Wait, I know of generative models which didn't need transport?
  - Dual formulation (need to motivate via examples what the dual refer to) 
  - Briefly mention about how the dual formulation connects with WGAN, MMDGAN, mention my own paper -> people understand that I am a credible person to talk about this.

# The flow view:
Question: why are we talking about "flow"? I thought we're talking about transporting which can very well mean just teleporting?
- Benamou-Brenier theorem (motivate how they connect with the flow view) -> by this point, people understand why we're talking about velocity

## How do we generate samples?
- Establish position, velocity, ODE formulation, Euler -> by this point, people understand that if we had velocity, we can sample
- Mention how to tweak this to make this into the SDE formulation (which is diffusion), Euler-Nadaraya -> same

## How do we train models?
- ODE: simple, learning velocity directly
- Need to motivate the score-based view via diagrams. How do we get velocity from the score?
-> by this point people understand how to train these models, why SDE sampling is slow, and why SDE is still theoretically OT while the ODE suffers from discretisation and estimation errors.

## Now tell me how we "actually" train these models?
- Latent variable modeling
- VAE, neural architecture

## Guidance
- By this point, people know how prompting works, or seeding (image-to-image), or image-to-video works

## Closing remarks: consistency models, distillation, fine-tuning.
